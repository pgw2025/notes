# 移动端编辑工具栏键盘遮挡改造 · 验证清单

> 配合 `plan.md`。仅在实施阶段使用，逐项勾选；本轮只出方案，未改代码。

## 一、静态代码核对（改后自检）

- [ ] `.mobile-toolbar` 定位已由 `sticky` 改为 `fixed`，`left/right/z-index` 保留
- [ ] 键盘收起时，工具栏 `bottom` 回落到底部安全区（含 `env(safe-area-inset-bottom)`）
- [ ] 键盘弹出时，工具栏 `bottom` 等于 `keyboardHeight`，且正确处理 `visualViewport.offsetTop`
- [ ] 精简态：H / B / I / 列表&待办 / 行内代码 仍显示，图片 / `···` 已隐藏
- [ ] 「收起键盘」按钮已加入精简态工具条最右端，点击后触发 `blur()`，`keyboardHeight` 归零
- [ ] 气泡菜单在收起键盘态可正常打开，精简单态不会误触发隐藏按钮

## 二、功能验证（真机 / 模拟器）

### 2.1 键盘弹出
- [ ] 点击正文输入框，键盘弹出后，工具栏贴合键盘顶部、不遮挡输入内容
- [ ] 精简态切换正确：图片与「更多」隐藏，收起键出现
- [ ] 最后一个字符录入时可完整看到（不被工具栏盖住）

### 2.2 一键收起键盘
- [ ] 点击「收起键盘」按钮，软键盘收起
- [ ] 收起后工具栏恢复为完整 7 键展开态
- [ ] 再次点击正文，键盘重新弹出，工具栏再次进入精简态

### 2.3 键盘收起/展开往复
- [ ] 连续多次展开→收起，工具栏位置无残留、无跳动、无错位
- [ ] 不使用 `position: fixed` 时的旧自适应逻辑残留（旧 bottom 偏移）已被清理

## 三、风格与体验验证

- [ ] 展开态工具栏毛玻璃与分割线样式正常（light / dark 主题均可见）
- [ ] 精简态工具条贴键盘上沿，圆角观感正常，无白边/锯齿
- [ ] 单手操作工具栏按钮间距仍可点按，无拥挤

## 四、回归测试

- [ ] 移动端笔记**新增**正常
- [ ] 移动端笔记**编辑**、**保存**正常
- [ ] 列表 / 待办 / 标题气泡打开、选择、关闭正常
- [ ] 图片上传入口仍可达（精简态收起键盘后仍可插入图片）
- [ ] 页面滚动时工具栏定位稳定，不遮挡正文、不误触
- [ ] 桌面端工具栏（`.desktop-toolbar`）不受本次改动影响

## 五、构建验收

- [ ] `frontend` 前端构建通过（无编译错误）
- [ ] 无新增未使用引用 / 死代码

---

## 备注

本轮为**方案设计**，未对代码做任何改动；以上清单仅作实施阶段验收依据。